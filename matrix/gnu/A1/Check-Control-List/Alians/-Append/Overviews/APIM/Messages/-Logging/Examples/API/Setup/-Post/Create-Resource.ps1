#!/home/admin/app/PowerShell/Members

# Alians Create a new array of member names from the CSV file
$members = Import-Csv -Path "members.csv" | Select-Object -ExpandProperty Member
Write-Host ./Web-Report.ps1 $members

# Alians Check if the CSV file exists
if (-not (Test-Path "members.csv")) {
    Write-Host "Error: members.csv not found."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}

# Alians Check if the PowerShell script is running as an administrator
if (-not (Test-Path -Path $PSScriptRoot -PathType Container)) {
    Write-Host "Error: PowerShell script must be run as an administrator."
    exit 1
}


