#!/home/admin/app/PowerShell/Members

# Alians Status - Member status and last update Off and On
$aliansStatus = @{
    'Alien1' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien2' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien3' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien4' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien5' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien6' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien7' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien8' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien9' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien10' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien11' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien12' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien13' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien14' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien15' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien16' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien17' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien18' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien19' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien20' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien21' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien22' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien23' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien24' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien25' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien26' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
    'Alien27' = @{
        'Status' = 'On'
        'LastUpdate' = (Get-Date).ToString('yyyy-MM-dd HH:mm:ss')
    }
}

# Get Alians Status
$aliansStatus
